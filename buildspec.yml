version: 0.2

phases:
  build:
    commands:
      - syft dir:.
      - grype .
       
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - webapp/target/*.war
    - server/target/*.jar
cache:
  paths:
    - '/root/.m2/**/*'
